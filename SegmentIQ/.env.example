# SegmentIQ Flask Configuration

# Secret Key for signing session cookies. 
# IN PRODUCTION: Keep this secret and set it to a secure, random string (e.g. 32/64 bytes hex).
# You can generate a random key in python using: import secrets; print(secrets.token_hex(32))
SECRET_KEY=your_secret_key_here
